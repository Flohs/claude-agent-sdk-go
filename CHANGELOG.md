# Changelog

## [Unreleased]

### Added

- `HookEventMessage` typed struct for hook lifecycle events (fields: `HookEvent`, `HookID`, `HookName`, `Output map[string]any`, `ExitCode *int`, `Outcome`). When `Options.IncludeHookEvents` is `true`, the CLI emits `hook_started` and `hook_response` system messages for each hook invocation; the SDK now parses these into `*HookEventMessage` (embedding `SystemMessage` for backward compatibility). Port of Python SDK PR anthropics/claude-agent-sdk-python#917. ([#328](https://github.com/Flohs/claude-agent-sdk-go/issues/328))
- `ApiRetryError` type and `ApiRetryErrorRateLimit` / `ApiRetryErrorOverloaded` constants for the machine-readable error category on `ApiRetryMessage`. `ApiRetryMessage` gains an `Error ApiRetryError` field (`json:"error,omitempty"`) exposing this value: `"overloaded"` for 529 and `"rate_limit"` for 429. Port of TypeScript SDK v0.3.150. ([#299](https://github.com/Flohs/claude-agent-sdk-go/issues/299))
- The `initialize` control request is now idempotent: a second call returns the cached initialization result instead of failing with an `"already initialized"` error, supporting reconnect scenarios. Control responses can now carry a `pending_permission_requests` field; the SDK dispatches each entry as a synthetic permission request so permission hook callbacks are not missed when they are queued before the session loop is fully running. Port of TypeScript SDK v0.3.161. ([#309](https://github.com/Flohs/claude-agent-sdk-go/issues/309))
- `StopDetails map[string]any` field on `AssistantMessage` and `ResultMessage`. Populated from the CLI's `stop_details` JSON key when present (e.g. when `StopReason` is `"refusal"`), enabling programmatic refusal detection without text-matching. Port of TypeScript SDK v0.3.162. ([#313](https://github.com/Flohs/claude-agent-sdk-go/issues/313))
- `AdvisorToolConfig` struct (fields: `Model`, `MaxUses`, `Caching`, `AllowedCallers`) and `Advisor any` field on `AgentDefinition`. Set `Advisor` to `true` to enable the advisor tool with default config, or provide `*AdvisorToolConfig` for fine-grained control. Enables the executor/advisor pattern without dropping to the raw Anthropic SDK. Port of Python SDK PR anthropics/claude-agent-sdk-python#880. ([#279](https://github.com/Flohs/claude-agent-sdk-go/issues/279))
- `AlwaysLoad bool` field (`json:"alwaysLoad,omitempty"`) on `McpStdioServerConfig`, `McpSSEServerConfig`, and `McpHTTPServerConfig`. When `true`, the CLI waits for the server to connect before executing the first query (blocking behavior). By default servers connect in the background since CLI v2.1.142. Port of TypeScript SDK v0.3.142. ([#272](https://github.com/Flohs/claude-agent-sdk-go/issues/272))
- `SandboxFilesystemConfig` struct with fields `AllowWrite`, `DenyWrite`, `DenyRead`, `AllowRead`, and `AllowManagedReadPathsOnly` for controlling filesystem access in sandboxed commands. Port of Python SDK PR anthropics/claude-agent-sdk-python#862. ([#280](https://github.com/Flohs/claude-agent-sdk-go/issues/280))
- `Filesystem *SandboxFilesystemConfig` and `EnableWeakerNetworkIsolation *bool` fields on `SandboxSettings`. `EnableWeakerNetworkIsolation` allows system TLS access on macOS when network isolation is enabled. Port of Python SDK PR anthropics/claude-agent-sdk-python#862. ([#280](https://github.com/Flohs/claude-agent-sdk-go/issues/280))
- `Timestamp string` field (ISO-8601, `json:"timestamp,omitempty"`) on `UserMessage`, `AssistantMessage`, `SystemMessage`, and `ResultMessage`. Populated from the `timestamp` field present in CLI transcript JSONL entries. Port of Python SDK PR anthropics/claude-agent-sdk-python#898. ([#277](https://github.com/Flohs/claude-agent-sdk-go/issues/277))
- `HookEventMessageDisplay` (`"MessageDisplay"`) hook event constant, `MessageDisplayHookInput` typed struct (fields: `TurnID`, `MessageID`, `Index`, `Final`, `Delta`), and `MessageDisplayHookOutput` typed output struct with `DisplayContent *string` and `ToHookJSONOutput()` helper. The `MessageDisplay` hook fires during assistant message streaming; returning a non-nil `DisplayContent` replaces the text shown to the user. Display-only: the stored message and what the model sees are untouched. Port of TypeScript SDK v0.3.152. ([#249](https://github.com/Flohs/claude-agent-sdk-go/issues/249))
- `HookEventSessionStart` (`"SessionStart"`) hook event constant, `SessionStartHookInput` typed struct, and `SessionStartHookOutput` typed output with `ReloadSkills bool`, `SessionTitle string`, and `ToHookJSONOutput()`. Returning `ReloadSkills: true` triggers a skill re-scan; `SessionTitle` sets the session title at initialization via `hookSpecificOutput.sessionTitle`. Port of TypeScript SDK v0.3.152. ([#250](https://github.com/Flohs/claude-agent-sdk-go/issues/250))
- `HookEventSessionEnd` (`"SessionEnd"`) hook event and `SessionEndHookInput` typed struct. Fires when a session ends (complements `SessionStart`). ([#252](https://github.com/Flohs/claude-agent-sdk-go/issues/252))
- `HookEventStopFailure` (`"StopFailure"`) hook event and `StopFailureHookInput` typed struct. Fires when the Stop hook itself encounters an error. ([#252](https://github.com/Flohs/claude-agent-sdk-go/issues/252))
- `HookEventPostCompact` (`"PostCompact"`) hook event and `PostCompactHookInput` typed struct with `Trigger` (`"manual"` or `"auto"`) and `CompactSummary` fields. Fires after a context compaction completes. ([#253](https://github.com/Flohs/claude-agent-sdk-go/issues/253))
- `HookEventPostToolBatch` (`"PostToolBatch"`) hook event, `PostToolBatchHookInput` struct (field: `ToolCalls []PostToolBatchToolCall`), and `PostToolBatchToolCall` type (`ToolName`, `ToolInput`, `ToolUseID`, `ToolResponse`). Fires once after a batch of tool calls completes, as opposed to `PostToolUse` which fires per tool. ([#254](https://github.com/Flohs/claude-agent-sdk-go/issues/254))
- `HookEventPermissionDenied` (`"PermissionDenied"`) hook event, `PermissionDeniedHookInput` struct (`ToolName`, `ToolInput`, `ToolUseID`, `Reason`), and `PermissionDeniedHookOutput` typed output with `Retry bool` and `ToHookJSONOutput()`. Fires when a tool call is blocked by a permission check; returning `Retry: true` asks the CLI to retry. ([#255](https://github.com/Flohs/claude-agent-sdk-go/issues/255))
- `HookEventElicitationResult` (`"ElicitationResult"`) hook event and `ElicitationResultHookInput` struct (`McpServerName`, `ElicitationID`, `Mode`, `Action`, `Content`). Fires when an MCP server elicitation request completes; complements `HookEventElicitation` which fires when the request is received. ([#256](https://github.com/Flohs/claude-agent-sdk-go/issues/256))
- `HookEventInstructionsLoaded` (`"InstructionsLoaded"`) hook event constant and `InstructionsLoadedHookInput` typed struct. Fires when Claude loads a CLAUDE.md or rules file during session initialization or directory traversal. Input fields: `FilePath`, `MemoryType` (`"User"`, `"Project"`, `"Local"`, `"Managed"`), `LoadReason` (`"session_start"`, `"nested_traversal"`, `"path_glob_match"`, `"include"`, `"compact"`), `Globs`, `TriggerFilePath`, `ParentFilePath`. ([#257](https://github.com/Flohs/claude-agent-sdk-go/issues/257))
- `HookEventCwdChanged` (`"CwdChanged"`) hook event constant and `CwdChangedHookInput` typed struct with `OldCwd` and `NewCwd` fields. Fires when the working directory changes during a session. ([#257](https://github.com/Flohs/claude-agent-sdk-go/issues/257))
- `HookEventFileChanged` (`"FileChanged"`) hook event constant and `FileChangedHookInput` typed struct with `FilePath` and `ChangeType` (`"modified"`, `"created"`, `"deleted"`) fields. Fires when a watched file changes. Watch paths are registered via the `SessionStart` hook output's `WatchPaths` field. ([#257](https://github.com/Flohs/claude-agent-sdk-go/issues/257))
- `HookEventWorktreeCreate` (`"WorktreeCreate"`) hook event constant, `WorktreeCreateHookInput` struct with `WorktreeName` and `IsolationLevel` fields, and `WorktreeCreateHookOutput` typed output struct. When the output's `WorktreePath` is non-empty the CLI uses that path instead of its default worktree location. ([#258](https://github.com/Flohs/claude-agent-sdk-go/issues/258))
- `HookEventWorktreeRemove` (`"WorktreeRemove"`) hook event constant and `WorktreeRemoveHookInput` struct with `WorktreePath` field. Fires when the CLI removes a git worktree; observability only. ([#258](https://github.com/Flohs/claude-agent-sdk-go/issues/258))
- `HookEventUserPromptExpansion` (`"UserPromptExpansion"`) hook event constant and `UserPromptExpansionHookInput` struct. Fires before each user prompt is processed, enabling prompt transformation or logging. ([#260](https://github.com/Flohs/claude-agent-sdk-go/issues/260))
- `ElicitationCompleteMessage` typed struct for MCP elicitation completion events. Fields: `RequestID`, `ServerName`, `Result map[string]any`. Parsed from system messages with subtype `elicitation_complete`. Port of TypeScript SDK v0.2.76. ([#261](https://github.com/Flohs/claude-agent-sdk-go/issues/261))
- `ExitPlanModeToolInput` typed struct (field: `PlanFilePath string`) for the `ExitPlanMode` tool. Port of TypeScript SDK v0.2.76. ([#262](https://github.com/Flohs/claude-agent-sdk-go/issues/262))
- `HookEventElicitation` (`"Elicitation"`) hook event constant and `ElicitationHookInput` struct (`McpServerName`, `ElicitationID`, `RequestedSchema`). Fires when an MCP server sends an elicitation request. ([#263](https://github.com/Flohs/claude-agent-sdk-go/issues/263))
- `MemoryRecallMessage` typed struct (field: `Paths []string`) for system messages with subtype `memory_recall`. Port of TypeScript SDK v0.2.105. ([#264](https://github.com/Flohs/claude-agent-sdk-go/issues/264))
- `SupportsFastMode bool` field on `ServerCapabilities`. Set when the CLI advertises fast-mode support during session init (e.g. Opus fast mode). Port of TypeScript SDK v0.2.69. ([#265](https://github.com/Flohs/claude-agent-sdk-go/issues/265))
- `MemoryPaths []string` field on `ServerCapabilities`. The CLI populates this with the memory file paths loaded at session initialization. Port of TypeScript SDK v0.2.105. ([#266](https://github.com/Flohs/claude-agent-sdk-go/issues/266))
- `StopDetails map[string]any` field on `AssistantMessage`. Populated from the CLI's `stop_details` JSON key when `StopReason` is `"refusal"` or another condition carrying extra context. ([#267](https://github.com/Flohs/claude-agent-sdk-go/issues/267))
- `DeferredToolUse` struct and `DeferredToolUse *DeferredToolUse` field on `ResultMessage`. Populated when a `PreToolUse` hook returns `{"decision": "defer"}`, surfacing the pending tool call to the caller. Fields: `ToolUseID`, `ToolName`, `ToolInput map[string]any`. ([#268](https://github.com/Flohs/claude-agent-sdk-go/issues/268))
- `HookDecision` type with constants `HookDecisionApprove`, `HookDecisionBlock`, `HookDecisionDefer`. Codifies the three valid decision values a `PreToolUse` hook can return. ([#268](https://github.com/Flohs/claude-agent-sdk-go/issues/268))
- `APIErrorStatus *int` field on `ResultMessage`. Carries the HTTP status code (e.g. 429, 529) from a failing API call when `IsError` is true. Populated from `api_error_status` in the CLI output. ([#269](https://github.com/Flohs/claude-agent-sdk-go/issues/269))
- `Origin string` field on `ResultMessage`. Forwards the triggering message's origin from the CLI (e.g. `"task_notification"`) so consumers can distinguish user-prompted results from task-notification followups. ([#270](https://github.com/Flohs/claude-agent-sdk-go/issues/270))
- `RequestID string` field on `AssistantMessage` and `ResultMessage`. Carries the API request identifier. ([#271](https://github.com/Flohs/claude-agent-sdk-go/issues/271))
- `SubagentType string` and `TaskDescription string` fields on `TaskStartedMessage`, `TaskProgressMessage`, and `TaskNotificationMessage`. `SubagentType` identifies the agent type (e.g. `"general-purpose"`, `"explore"`); `TaskDescription` is a human-readable task summary. ([#273](https://github.com/Flohs/claude-agent-sdk-go/issues/273))
- `MirrorErrorMessage` SDK-synthesized system message type. Never originates from the CLI — injected by the SDK when the transcript mirror batcher exhausts its retry budget. Fields: `Key *SessionKey`, `Error string`, `UUID string`, `SessionID string`. ([#274](https://github.com/Flohs/claude-agent-sdk-go/issues/274))
- `ApiRetryMessage` typed struct for `api_retry` system messages. Fields: `AttemptNumber`, `MaxAttempts`, `DelayMs`, `ErrorStatus *int`, `ErrorMessage string`. Port of TypeScript SDK v0.2.77. ([#275](https://github.com/Flohs/claude-agent-sdk-go/issues/275))
- `ServerToolName` type and constants (`ServerToolAdvisor`, `ServerToolWebSearch`, `ServerToolWebFetch`, `ServerToolCodeExecution`, `ServerToolBashCodeExecution`, `ServerToolTextEditorCodeExecution`, `ServerToolSearchRegex`, `ServerToolSearchBM25`) for server-side tool names. `ServerToolUseBlock` and `ServerToolResultBlock` content block types for server-side tool use and results. ([#276](https://github.com/Flohs/claude-agent-sdk-go/issues/276))
- `Base64Source` and `Base64Block` types for image and document content blocks in multimodal messages. `NewBase64Content` helper infers block type from media type prefix. ([#278](https://github.com/Flohs/claude-agent-sdk-go/issues/278))
- `TaskCreateInput`, `TaskCreateOutput`, `TaskGetInput`, `TaskGetOutput`, `TaskUpdateInput`, `TaskUpdateOutput`, `TaskListInput`, `TaskListOutput` typed structs for the four task management tools (`TaskCreate`, `TaskGet`, `TaskUpdate`, `TaskList`). `TaskStatus` type with `TaskStatusPending`, `TaskStatusInProgress`, `TaskStatusCompleted`, `TaskStatusDeleted` constants. ([#281](https://github.com/Flohs/claude-agent-sdk-go/issues/281))
- `SessionStoreSubkeys` interface and `ListSubkeys` method for listing sibling sub-streams (e.g. subagent transcripts) under a session. `SessionListSubkeysKey` struct identifies the parent session. ([#282](https://github.com/Flohs/claude-agent-sdk-go/issues/282))
- `SessionStoreSummarizer` interface with `LoadSummary` / `SaveSummary` methods and `SessionSummaryEntry` type. `FoldSessionSummary` function folds a session transcript into a summary map for efficient staleness checks. ([#283](https://github.com/Flohs/claude-agent-sdk-go/issues/283))
- `ReadStateEntry` struct and `SeedReadState([]ReadStateEntry)` method on `Client`. Lets callers pre-populate the CLI's read-state cache with file paths and mtimes so Edit-style tools work across context compactions without requiring a fresh `Read`. ([#284](https://github.com/Flohs/claude-agent-sdk-go/issues/284))
- `SDKSessionInfo` struct returned by `ListSessions` and `GetSessionInfo`. New fields: `Tag *string` (session tag), `CreatedAt *int64` (creation timestamp in Unix milliseconds). ([#285](https://github.com/Flohs/claude-agent-sdk-go/issues/285))
- `ThinkingConfigAdaptive` mode and `SupportsAdaptiveThinking bool` on `ServerCapabilities`. Enables adaptive thinking on supported models; `ServerCapabilities` now reports whether the active model supports it. ([#286](https://github.com/Flohs/claude-agent-sdk-go/issues/286))
- `StructuredOutput any` field on `ResultMessage`. Carries the structured JSON output when `Options.OutputFormat` is `"json"` or `"stream-json"`. ([#287](https://github.com/Flohs/claude-agent-sdk-go/issues/287))
- `ContextUsage` struct with fields `TotalTokens`, `UsedTokens`, `UsageByCategory map[string]int`. Returned by a new `GetContextUsage()` method on `Client` that queries the CLI's `/context-usage` endpoint. ([#288](https://github.com/Flohs/claude-agent-sdk-go/issues/288))
- `SandboxSettings.ContainerEngine string` field for selecting the container runtime (e.g. `"docker"`, `"podman"`). ([#289](https://github.com/Flohs/claude-agent-sdk-go/issues/289))
- `Options.MCPAllowedTools []string` field. Passed to the CLI as `--mcp-tool-allowlist` to restrict which MCP server tools are available during a session. ([#290](https://github.com/Flohs/claude-agent-sdk-go/issues/290))
- `Options.AgentProgressSummaries bool` field. When `true`, passes `--agent-progress-summaries` to the CLI so `TaskProgressMessage.Summary` is populated with an AI-generated progress description. ([#291](https://github.com/Flohs/claude-agent-sdk-go/issues/291))
- `Options.IncludeHookEvents bool` field. When `true`, passes `--include-hook-events` to the CLI so hook lifecycle messages (`hook_started`, `hook_response`) are emitted into the message stream. ([#292](https://github.com/Flohs/claude-agent-sdk-go/issues/292))
- `Options.FastMode bool` field. When `true`, passes `--fast` to the CLI to enable fast mode (e.g. Haiku-backed Opus fast mode). Port of TypeScript SDK v0.2.69. ([#293](https://github.com/Flohs/claude-agent-sdk-go/issues/293))
- `Options.Effort Effort` field and `Effort` type with `EffortLow`, `EffortNormal`, `EffortHigh` constants. Passed as `--effort` to the CLI for models that support it. Port of TypeScript SDK v0.3.140. ([#294](https://github.com/Flohs/claude-agent-sdk-go/issues/294))
- `Options.ThinkingConfig ThinkingConfig` field, `ThinkingConfig` interface, `ThinkingConfigEnabled` struct (field: `BudgetTokens int`), and `ThinkingConfigAdaptive` / `ThinkingConfigDisabled` constants. Passed as `--thinking-budget` or `--thinking` to the CLI. Port of TypeScript SDK v0.3.118. ([#295](https://github.com/Flohs/claude-agent-sdk-go/issues/295))
- `Options.OutputFormat string` field. Passed as `--output-format` to the CLI; useful for structured JSON output. ([#296](https://github.com/Flohs/claude-agent-sdk-go/issues/296))
- `SupportedEffortLevels []Effort` field on `ServerCapabilities`. Lists the effort levels (`low`, `normal`, `high`) the currently active model accepts. ([#297](https://github.com/Flohs/claude-agent-sdk-go/issues/297))
- `NewTextContent` / `NewBase64Content` helpers and `SendQueryWithContent(ctx, []map[string]any, ...Option)` method on `Client` for multimodal queries. ([#298](https://github.com/Flohs/claude-agent-sdk-go/issues/298))

### Fixed

- `mapField` helper now correctly extracts `map[string]any` values from message data maps, fixing silent nil returns when the value exists but isn't typed as `map[string]any` directly. ([#300](https://github.com/Flohs/claude-agent-sdk-go/issues/300))

---

## [0.1.0] — 2025-01-01

### Added

- Initial release of the Claude Agent SDK for Go.
- `Client` struct with `Query`, `SendQuery`, `ReceiveMessages`, `Close` methods for interacting with the Claude CLI subprocess.
- `ParseMessage` for deserializing CLI JSONL output into typed Go structs: `UserMessage`, `AssistantMessage`, `SystemMessage`, `ResultMessage`, `StreamEvent`, `RateLimitEvent`.
- `Options` struct with fields: `Model`, `MaxTurns`, `SystemPrompt`, `AppendSystemPrompt`, `AllowedTools`, `DisallowedTools`, `McpServers`, `PermissionMode`, `Cwd`, `SessionID`, `ContinueSession`, `ResumeSession`, `CustomInstructions`, `Verbose`, `DebugCLI`.
- Hook system: `PreToolUseHookInput`, `PostToolUseHookInput`, `StopHookInput`, and typed output structs (`PreToolUseHookOutput`, `PostToolUseHookOutput`, `StopHookOutput`) with `ToHookJSONOutput()` helpers for hook callbacks.
- `HookEvent` constants: `HookEventPreToolUse`, `HookEventPostToolUse`, `HookEventStop`.
- `SessionStore` interface and in-memory adapter (`MemorySessionStore`) for persisting session transcripts.
- `SessionStoreLister` interface with `ListSessions` for retrieving stored session metadata.
- `ProjectKeyForDirectory` for deriving per-project storage namespaces.
- MCP server config types: `McpStdioServerConfig`, `McpSSEServerConfig`, `McpHTTPServerConfig`.
- `PermissionMode` type with `PermissionModeDefault`, `PermissionModeAutoEdit`, `PermissionModeBypassPermissions` constants.
- `AssistantMessageError` type with constants for authentication, billing, rate-limit, and server errors.
- `MessageParseError` and `SDKError` error types.
- `SandboxSettings` with `Enabled bool`, `Network bool`, `Image string`, `BindMounts []string`, `EnvVars map[string]string`.
- `AgentDefinition` struct for defining sub-agents with `Name`, `Description`, `Prompt`, `Model`, `Tools`, `MCPServers`, `MaxTurns`, `SystemPrompt`, `AppendSystemPrompt`, `AllowedTools`, `DisallowedTools`, `Hooks`, `Sandbox`, `PermissionMode`, `OutputFormat`, and `SubagentType` fields.
- `HookConfig` for registering hook callbacks, with support for Go function callbacks and external process hooks.
- `SessionMessage`, `SessionKey`, `SessionStoreEntry`, `SessionStoreListEntry` types for session management.
- `ServerCapabilities` struct with `SupportsEffort bool` and `SupportedEffortLevels []Effort`.

### Documentation

- Document minimum CLI version requirement (>= 2.0.0) in README. ([#3](https://github.com/Flohs/claude-agent-sdk-go/issues/3))
